# Maximum Segment Score

给定长度为 `n` 的整数序列，求一个非空连续子段的最大元素和。

## 输入格式

第一行包含整数 `n`，其中 `1 <= n <= 200000`。

第二行包含 `n` 个整数 `a_i`，其中 `-10^9 <= a_i <= 10^9`。

## 输出格式

输出一个整数，表示最大连续子段和。

## 样例

输入：

```text
5
-2 3 -1 4 -5
```

输出：

```text
6
```
