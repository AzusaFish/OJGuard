interface:
  display_name: "Audit Reference Solution"
  short_description: "Turn solution risks into evidence-ready hypotheses"
  default_prompt: "Use $audit-reference-solution to audit the reference solution against the problem contract."
dependencies:
  tools:
    - type: "mcp"
      value: "ojguard"
      description: "OJGuard source and compiler-diagnostic tools"
      transport: "streamable_http"
      url: "http://ojguard-mcp:8020/mcp"
